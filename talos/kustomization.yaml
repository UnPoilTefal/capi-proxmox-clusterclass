# =============================================================================
# Kustomize — ClusterClass Talos (placeholder)
#
# TalosControlPlaneTemplate n'est PAS implémenté dans CACPPT v0.5.x (dernière
# release : v0.5.13). La ClusterClass Talos est donc impossible avec ce provider.
#
# Le cluster app-talos utilise des manifestes standalone dans :
#   capi-bootstrap/clusters/app-talos/cluster.yaml
#
# Les fichiers talos/ sont conservés comme référence pour le jour où CACPPT
# ajoutera TalosControlPlaneTemplate (ou pour une migration vers un fork).
# clusterclass.yaml et talos-control-plane-template.yaml sont EXCLUS ici.
# =============================================================================
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: caprox-kubernetes-engine

resources: []
