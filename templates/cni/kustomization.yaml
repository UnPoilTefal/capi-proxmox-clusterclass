# CNI addon delivery — Calico activé (Cilium disponible mais commenté ci-dessous)
# Pour switcher vers Cilium : remplacer calico par cilium dans resources.
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
- helmchartproxy-calico.yaml
# - helmchartproxy-cilium.yaml              # activer pour Cilium
# - helmchartproxy-cilium-ipv6-dual-stack.yaml  # optionnel : dual-stack IPv4/IPv6
