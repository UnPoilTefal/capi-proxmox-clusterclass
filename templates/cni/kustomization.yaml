# CNI addon delivery — Calico activé par défaut.
# Pour switcher vers Cilium : remplacer par examples/helmchartproxy-cilium.yaml
# et changer le label caprox.eu/cni: calico → cilium sur le Cluster CR.
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
- helmchartproxy-calico.yaml
